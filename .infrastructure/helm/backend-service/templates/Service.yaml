apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: default
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - port: {{ .Values.containerPort }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
  type: ClusterIP