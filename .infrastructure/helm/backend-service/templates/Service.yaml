apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: default
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - port: {{ .containerPort }}
      targetPort: {{ .targetPort }}
      protocol: TCP
  type: ClusterIP